

const x: string[] = ['A', 'B', 'C', 'B', 'D', 'A', 'B'];
const y: string[] = ['B', 'D', 'C', 'A', 'B', 'A'];


function LCS_Length(x: string[], y: string[]): string[] {
    let c: number[][]
    for(let i = 0; i < x.length; i++) {

    }
    return [];
}